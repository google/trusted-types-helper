{
  "name": "trusted-types-helper",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "(cd ui && ng build) && (cd background && tsc)",
    "test": "(cd ui && ng build) && (cd background && tsc) && jest test/",
    "test-page": "node test/test_server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@angular/cli": "^18.0.5",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "jest-dev-server": "^10.0.0",
    "jest-puppeteer": "^10.0.1",
    "puppeteer": "^22.13.0",
    "ts-jest": "^29.2.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@types/chrome": "^0.0.268"
  }
}
